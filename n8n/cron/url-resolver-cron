# URL Resolver — times in UTC (VPS timezone)
# Runs every 30 minutes to resolve missing application URLs

*/30 * * * * docker exec oc python3 /home/node/.openclaw/skills/url-resolver/scripts/resolver.py >> /var/log/url-resolver.log 2>&1
