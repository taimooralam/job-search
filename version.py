"""
Version information for the Job Search application.

This file is the single source of truth for version numbers.
Both frontend and runner import from here.
"""

__version__ = "1.0.5"
__version_info__ = (1, 0, 5)

# Build metadata (set by CI/CD or manually)
BUILD_DATE = "2025-12-01"
GIT_COMMIT = None  # Will be set at runtime if available
